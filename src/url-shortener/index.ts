/**
 * User can add a url that needs to be shortened
 * A mapping of every url to it's shortened url
 * When a user visits a shortened url, they are redirected to the original url
 */

console.log(`lld of url shortener`)